mined is a MINimal text EDitor
Use make to build
And make run to run
No dependencies (Besides standard C library).
Guide
	You write characters in an overwrite mode (writing on a character
	overwrites it, delete replaces character with space, sets cursor
	back one space).
	
	Change cursor with arrow keys.
	Press escape to save file.
	To open file, do: ./main file
	If you do this, pressing escape saves opened file.
	If you open a nonexistant file, mined will create an empty file
	and saving it will create a new file.
